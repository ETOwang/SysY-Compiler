// Invalid syntax test
// TEST_CONFIG: target=arm,riscv; optimization=O0

// This test contains syntax errors that should be caught by the compiler

int main() {
    // Missing semicolon
    int x = 10
    
    // Invalid operator
    int y = 5 $ 2;
    
    // Missing closing brace for function
    return 0;
} 